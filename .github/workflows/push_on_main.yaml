name: Push on main

on:
  push:
    branches:
      - main

jobs:
  ci:
    permissions:
      contents: read
      # package_ourself pushes the pre-built CI image to ghcr.io on main.
      packages: write
    uses: ./.github/workflows/ci.yaml
    secrets: inherit
